import { useState } from 'react';
import useAppContext from '../context/useAppContext';

const Checkout = ({ item }) => {
    return (
        <div>
            <div>Checkout</div>
        </div>
    );
};

export default Checkout;
